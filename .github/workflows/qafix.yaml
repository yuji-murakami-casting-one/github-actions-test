name: qafix

on:
  pull_request_target:
    types:
      - opened

jobs:
  edit-pr:
    if:  startsWith(github.head_ref, 'qafix/')
    steps:
      - name: edit pull-request
        run: |
          gh pr edit --base qa
